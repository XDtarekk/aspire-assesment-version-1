# Backend API base URL (no trailing slash)
NEXT_PUBLIC_API_URL=http://localhost:3001

# Google OAuth Client ID (for Sign in with Google; same as backend GOOGLE_CLIENT_ID)
NEXT_PUBLIC_GOOGLE_CLIENT_ID=
